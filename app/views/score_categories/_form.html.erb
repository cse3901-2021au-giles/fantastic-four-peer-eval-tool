<%= form_with(model: score_category) do |form| %>
  <% if score_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score_category.errors.count, "error") %> prohibited this score_category from being saved:</h2>

      <ul>
        <% score_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
