<%= form_with(model: peer_evaluation) do |form| %>
  <% if peer_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peer_evaluation.errors.count, "error") %> prohibited this peer_evaluation from being saved:</h2>

      <ul>
        <% peer_evaluation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <select class="form-select" aria-label="Default select example">
    <option selected>Reviewer</option>
    <%= form.collection_select :team_id, Team.order(:name), :id, :name %>
    <%# TO-DO fix drop down %>
  </select>

  <select class="field" aria-label="Default select example">
    <%= form.collection_select :team_id, Team.order(:id), :id, :name %>
    <%# TO-DO fix drop down %>
  </select>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
