<%= form_with(model: review) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <p>Project</p>
  <%= form.collection_select :project_id, Project.order(:name), :id, :name, prompt:true %>
  <p>Reviewee</p>
  <%= form.collection_select :reviewee_id, Reviewee.order(:name), :id, :name, prompt:true %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
